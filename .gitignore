# Credentials. .env holds a live API key.
.env
.env.*
!.env.example
*.key

# The task briefs are weon's own documents � no reason to send them back.
*.pdf

# Working material, not part of the submission (see _archive/README.md)
_archive/
weon-submission.zip

# Personal interview prep. Never push this: the repo link goes to the reviewer, and this file
# is written to me, not to them.
REPORT_PERSONAL.md

# Python
__pycache__/
*.py[cod]
.pytest_cache/
.venv/
venv/

# Editor/OS
.DS_Store
Thumbs.db
.agents/

# Debris from tests/test_evidence_immutable.py (runs with WEON_OUT_SUFFIX=guardtest)
weon-pipeline/outputs/task4_guardtest/
weon-pipeline/outputs/*_run.log
